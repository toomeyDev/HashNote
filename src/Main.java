
public class Main {

	public static void main(String[] args) {
		// create instance of HashNote type to test functionality
		HashNote notes = new HashNote();
		System.out.println(notes.getNote(null));
		notes.printNote("Sample Note");
	}
}
